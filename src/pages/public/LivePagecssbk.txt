/*-- src/pages/site/LivePage.css --*/

/* ------------------------------
   GLOBAL PAGE WRAPPER
------------------------------ */
.live-wrapper {
  background: #000 !important;
  min-height: 100vh;
  padding: 30px 0 60px;
  color: #fff !important;
  width: 100%;
  box-sizing: border-box;
}

/* ------------------------------
   PAGE TITLE + STATUS
------------------------------ */
.live-title {
  text-align: center;
  font-size: 2.2rem;
  font-weight: 700;
  margin-bottom: 10px;
  color: #fff;
}

.live-status {
  text-align: center;
  font-size: 1.1rem;
  margin-bottom: 22px;
}

.live-status.online {
  color: #ff3b3b !important;
  font-weight: 700;
}

.live-status.offline {
  color: #bbb !important;
}

/* ------------------------------
   CENTER PLAYER CONTAINER
------------------------------ */
.live-player-shell {
  max-width: 900px; /* Adjust player width */
  margin: 0 auto;
  width: 100%;
}

/* ------------------------------
   PLAYER FRAME
------------------------------ */
.live-player {
  width: 100%;
  aspect-ratio: 16 / 9;
  background: #0a0a0a;
  border-radius: 14px;
  overflow: hidden;
  box-shadow: 0 0 28px rgba(0, 0, 0, 0.6);
}

.live-player iframe {
  width: 100%;
  height: 100%;
  border: none;
}

/* ------------------------------
   OFFLINE PLAYER WRAPPER
------------------------------ */
.live-player-offline {
  width: 100%;
  aspect-ratio: 16/9;
  background: #111;
  border: 1px solid #1d1d1d;
  border-radius: 14px;
  display: grid;
  place-items: center;
  text-align: center;
  padding: 20px;
  box-sizing: border-box;
}

/* ------------------------------
   OFFLINE MESSAGE
------------------------------ */
.live-offline-box {
  color: #fff !important;
  font-size: 1.1rem;
  line-height: 1.6rem;
}

/* ------------------------------
   COUNTDOWN OVERLAY BLOCK
------------------------------ */
.live-countdown {
  text-align: center;
  color: #fff;
  padding: 20px;
}

.live-countdown-label {
  font-size: 1rem;
  opacity: 0.8;
  margin-bottom: 8px;
}

.live-countdown-time {
  font-size: 2.4rem;
  font-weight: 700;
  letter-spacing: 2px;
  color: #a855f7; /* Purple accent */
}

/* ------------------------------
   LIVE BADGE ANIMATION (future-ready)
------------------------------ */
.live-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 1rem;
  font-weight: 700;
  color: #ff3b3b;
}

.live-badge::before {
  content: "";
  width: 12px;
  height: 12px;
  background: #ff3b3b;
  border-radius: 50%;
  display: inline-block;
  animation: pulse 1.3s infinite ease-in-out;
}

@keyframes pulse {
  0% {
    transform: scale(0.8);
    opacity: 0.7;
  }
  50% {
    transform: scale(1.1);
    opacity: 1;
  }
  100% {
    transform: scale(0.8);
    opacity: 0.7;
  }
}

/* ------------------------------
   LOADING MESSAGE
------------------------------ */
.live-loading {
  text-align: center !important;
  padding-top: 40px;
  font-size: 1.2rem;
  opacity: 0.8;
  color: #fff !important;
}

/* ------------------------------
   MOBILE RESPONSIVE
------------------------------ */
@media (max-width: 768px) {
  .live-title {
    font-size: 1.7rem;
  }

  .live-countdown-time {
    font-size: 2rem;
  }

  .live-player-shell {
    max-width: 100%;
  }

  .live-player,
  .live-player-offline {
    border-radius: 10px;
  }
}

.live-countdown,
.live-countdown-label,
.live-countdown-time {
  color: #fff !important;
}

.live-countdown-time {
  font-size: 30px;
}
