/* ============================================================================
   GLOBAL BASE
============================================================================ */
.home-root {
  background: #000;
  color: #fff;
  font-family: "Inter", sans-serif;
  overflow-x: hidden;
}

:root {
  --accent: #7312ad;
  --accent-glow: 0 0 22px rgba(115, 18, 173, 0.65);
  --line: rgba(255, 255, 255, 0.1);
  --muted: #b2b2b2;
}

/* ============================================================================
   HERO + HEADER
============================================================================ */
.hero-section {
  position: relative;
  min-height: 92vh;
  display: grid;
  align-items: center;
  padding-bottom: 60px;
  overflow: hidden;
  background: radial-gradient(
      circle at 30% 20%,
      rgba(115, 18, 173, 0.15),
      transparent 50%
    ),
    #000;
}

/* Subtle animated purple particles */
.hero-section::before,
.hero-section::after {
  content: "";
  position: absolute;
  border-radius: 50%;
  filter: blur(60px);
  opacity: 0.18;
  animation: float 12s ease-in-out infinite alternate;
}
.hero-section::before {
  width: 420px;
  height: 420px;
  background: #7312ad;
  top: -60px;
  left: -120px;
}
.hero-section::after {
  width: 360px;
  height: 360px;
  background: #4a0d78;
  bottom: -80px;
  right: -120px;
}

@keyframes float {
  from {
    transform: translateY(0px) translateX(0px);
  }
  to {
    transform: translateY(30px) translateX(40px);
  }
}

/* Over-darken hero for clarity */
.hero-overlay {
  position: absolute;
  inset: 0;
  backdrop-filter: blur(1px);
  background: linear-gradient(180deg, rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 1));
  z-index: 0;
}

/* ============================================================================
   HEADER (inside hero)
============================================================================ */
.hero-header {
  position: absolute;
  top: 20px;
  width: 100%;
  padding: 0 32px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  z-index: 2;
}

.hero-logo {
  font-weight: 900;
  font-size: 20px;
  letter-spacing: 0.5px;
  color: #fff;
}

.hero-nav {
  display: flex;
  align-items: center;
  gap: 20px;
}

.hero-nav-link {
  color: #fff;
  opacity: 0.8;
  text-decoration: none;
  font-weight: 600;
  transition: 0.2s;
}
.hero-nav-link:hover {
  opacity: 1;
}

.hero-nav-btn {
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 800;
  text-decoration: none;
  color: #fff;
  background: var(--accent);
  box-shadow: var(--accent-glow);
  transition: 0.25s;
}
.hero-nav-btn:hover {
  background: #5a0f8b;
}

/* ============================================================================
   HERO CONTENT
============================================================================ */
.hero-content {
  position: relative;
  z-index: 2;
  padding: 60px 32px;
  max-width: 720px;
  margin-top: 80px;
}

.hero-kicker {
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: #d6a5ff !important;
  font-size: 13px;
  margin-bottom: 14px;
  font-weight: 700;
  opacity: 0.85;
}

.hero-title {
  margin: 0;
  font-weight: 900;
  font-size: clamp(34px, 6vw, 60px);
  line-height: 1.1;
  text-shadow: 0 2px 14px rgba(115, 18, 173, 0.55);
  color: #fff !important;
}

.hero-sub {
  margin: 16px 0 24px;
  font-size: clamp(16px, 2.4vw, 20px);
  color: #dcdcdc;
  max-width: 580px;
  line-height: 1.5;
}

.hero-cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 14px 28px;
  text-decoration: none;
  color: #fff;
  background: var(--accent);
  font-weight: 900;
  border-radius: 10px;
  box-shadow: var(--accent-glow);
  transition: 0.25s;
}
.hero-cta:hover {
  background: #5a0f8b;
  transform: translateY(-2px);
}

/* ============================================================================
   JOIN STRIP
============================================================================ */
.join-strip {
  padding-top: 40px !important;
  padding-bottom: 100px !important;
  /* border-top: 1px solid var(--line); */
  background: #050505;
  border: none !important;
}

.join-inner {
  width: min(1100px, 92vw);
  margin: auto;
  /* display: flex; */
  /* /* justify-content: space-between; */
  text-align: center;
  flex-wrap: wrap;
  gap: 18px;
  align-items: center;
}

.join-inner h5 {
  color: #fff !important;
}

.join-title {
  margin: 0;
  font-size: 28px;
  font-weight: 900;
  color: #fff !important;
}

.join-sub {
  margin-top: 6px;
  opacity: 0.8;
  padding-bottom: 30px;
  color: #fff !important;
}

.join-btn {
  padding: 12px 20px;
  background: var(--accent);
  border-radius: 8px;
  font-weight: 800;
  color: #fff;
  text-decoration: none;
  box-shadow: var(--accent-glow);
}
.join-btn:hover {
  background: #5a0f8b;
}

/* ============================================================================
   FEATURE HIGHLIGHT
============================================================================ */
.feature-section {
  position: relative;
  padding: 80px 0;
  background: #0a0a0a;
  text-align: center;
  border: none !important;
}

.feature-bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.2;
}

.feature-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(0, 0, 0, 0.6), #000);
}

.feature-content {
  position: relative;
  z-index: 2;
}

.feature-title {
  font-size: 28px;
  font-weight: 900;
  margin-bottom: 18px;
  color: #fff !important;
  margin-bottom: 30px !important;
}

.feature-btn {
  padding: 12px 22px;
  background: var(--accent);
  border-radius: 8px;
  font-weight: 800;
  text-decoration: none;
  color: #fff;
  box-shadow: var(--accent-glow);
}
.feature-btn:hover {
  background: #5a0f8b;
}

/* ============================================================================
   LATEST TEACHING
============================================================================ */
.latest-section {
  padding: 80px 0;
  text-align: center;
  width: min(1300px, 92vw);
  margin: auto;
}

.latest-title {
  font-size: 32px;
  font-weight: 900;
}

.latest-sub {
  margin-top: 10px;
  color: #bbb;
}

/* GRID */
.latest-grid {
  margin-top: 40px;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 28px;
  justify-items: center;
}

/* CARD */
.latest-card {
  width: 100%;
  background: #0d0d0d;
  border-radius: 12px;
  overflow: hidden;
  text-decoration: none;
  color: #fff;
  border: 1px solid rgba(255, 255, 255, 0.07);
  transition: 0.25s ease;
}

.latest-card:hover {
  transform: translateY(-6px);
  border-color: var(--accent);
  box-shadow: 0 14px 30px rgba(0, 0, 0, 0.55);
}

/* THUMBNAIL */
.latest-thumb img {
  width: 100%;
  height: 170px;
  object-fit: cover;
  display: block;
}

/* META INFO */
.latest-meta {
  padding: 14px;
  text-align: left;
}

.latest-video-title {
  margin: 0;
  font-size: 15px;
  font-weight: 800;
  line-height: 1.3;
  color: #fff !important;
}

.latest-category {
  margin-top: 6px;
  font-size: 12px;
  opacity: 0.6;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: #fff !important;
}

/* LOADING */
.latest-loading {
  margin-top: 40px;
  font-size: 18px;
  opacity: 0.7;
}

/* ============================================================================
   RESPONSIVE GRID FIXES
============================================================================ */

/* TABLET */
@media (max-width: 1024px) {
  .latest-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* BIG MOBILE */
@media (max-width: 820px) {
  .latest-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* MOBILE */
@media (max-width: 520px) {
  .latest-grid {
    grid-template-columns: 1fr;
  }

  .latest-thumb img {
    height: 180px;
  }
}

/* ============================================================================
   WATCH MORE CTA
============================================================================ */
.cta-section {
  text-align: center;
  padding: 70px 0 90px;
  background: #0b0b0b;
}

.cta-title {
  margin: 0;
  font-size: 30px;
  font-weight: 900;
}

.cta-sub {
  max-width: 760px;
  margin: 10px auto 20px;
  opacity: 0.8;
  padding-bottom: 20px;
}

.cta-button {
  padding: 14px 28px;
  background: var(--accent);
  text-decoration: none;
  color: #fff;
  font-weight: 800;
  border-radius: 10px;
  box-shadow: var(--accent-glow);
}
.cta-button:hover {
  background: #5a0f8b;
}

/* ============================================================================
   ABOUT SECTION
============================================================================ */
.about-section {
  padding: 80px 0;
  display: flex;
  width: min(1000px, 92vw);
  margin: auto;
  align-items: center;
  gap: 22px;
  border-top: 1px solid var(--line);
}

.about-avatar {
  width: 150px;
  height: 110px;
  border-radius: 100px;
  object-fit: contain;
  border: 2px solid var(--accent);
  box-shadow: var(--accent-glow);
}

.about-name {
  margin: 0;
  font-size: 24px;
  font-weight: 900;
  color: #fff !important;
}

.about-text {
  margin-top: 8px;
  color: #ccc;
  line-height: 1.6;
}

/* ============================================================================
   RESPONSIVE
============================================================================ */
@media (max-width: 720px) {
  .hero-header {
    padding: 0 18px;
  }

  .about-section {
    flex-direction: column;
    text-align: center;
  }
}

.video-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 22px;
  margin-top: 30px;
}

.video-card {
  background: #0d0d0d;
  border: 1px solid #222;
  border-radius: 12px;
  padding-bottom: 10px;
  text-decoration: none;
  color: #fff;
  transition: 0.2s;
}

.video-card:hover {
  transform: translateY(-4px);
  border-color: #7312ad;
}

.video-thumb img {
  width: 100%;
  height: 150px;
  object-fit: cover;
  border-radius: 10px 10px 0 0;
}

.video-info {
  padding: 10px 12px;
}

.video-title {
  font-size: 15px;
  font-weight: 700;
}

.video-date {
  color: #ccc;
  font-size: 13px;
  margin-top: 4px;
}

/* Hover Play Icon */
.hover-card {
  position: relative;
  overflow: hidden;
}

.hover-card .hover-play {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  transition: 0.25s ease;
}

.hover-card:hover .hover-play {
  opacity: 1;
}

.hover-card .hover-play span {
  font-size: 42px;
  font-weight: 900;
  color: #fff;
  text-shadow: 0 0 12px rgba(115, 18, 173, 0.9);
  transform: scale(0.8);
  transition: 0.25s ease;
}

.hover-card:hover .hover-play span {
  transform: scale(1);
}

.home-live-countdown {
  margin-top: 10px;
  margin-bottom: 10px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  color: #f9fafb;
  font-size: 14px;
}

.home-live-countdown-label {
  opacity: 0.8;
  color: #fff !important;
  font-size: 20px;
}

.home-live-countdown-time {
  font-weight: 700;
  letter-spacing: 0.05em;
  color: #fff !important;
  font-size: 35px;
}

/* Make the logo fit nicely and stay readable */
.latest-thumb.is-default-thumb img {
  object-fit: contain !important;
  object-position: center !important;
  padding: 18px !important; /* more breathing room */
  transform: none !important;
  filter: drop-shadow(0 6px 18px rgba(0, 0, 0, 0.65));
}

/* Optional: give it a subtle “card” feel inside the thumb */
.latest-thumb.is-default-thumb::after {
  content: "";
  position: absolute;
  inset: 10px;
  border-radius: 14px;
  /* border: 1px solid rgba(140, 70, 255, 0.35); */
  /* box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.04); */
  pointer-events: none;
}
